import { defineConfig } from "tsup"; export default defineConfig({ entry: ["src/index.ts"], format: ["esm"], dts: process.env.NODE_ENV === "production", splitting: false, sourcemap: process.env.NODE_ENV === "production", clean: true });
